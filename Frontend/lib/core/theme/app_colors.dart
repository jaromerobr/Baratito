// Baratito color palette — Verde Bosque & Amarillo Mostaza.
import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Brand colors — Verde Bosque ────────────────────────
  static const Color primary = Color(0xFF2D6A4F);        // Forest green
  static const Color primaryLight = Color(0xFF40916C);
  static const Color primaryDark = Color(0xFF1B4332);

  static const Color accent = Color(0xFFD4A017);          // Mustard yellow
  static const Color accentLight = Color(0xFFE8B931);
  static const Color accentDark = Color(0xFFB8860B);

  // ── Backgrounds ───────────────────────────────────────
  static const Color backgroundLight = Color(0xFFF7F5F0);  // Warm off-white
  static const Color backgroundDark = Color(0xFF1A1A2E);

  static const Color surfaceLight = Color(0xFFFFFFFF);
  static const Color surfaceDark = Color(0xFF252540);

  // ── Header gradient ───────────────────────────────────
  static const Color headerYellow = Color(0xFFD4A017);     // Mustard
  static const Color headerGreen = Color(0xFF2D6A4F);      // Forest

  // ── Text ──────────────────────────────────────────────
  static const Color textPrimary = Color(0xFF2D2D3A);
  static const Color textSecondary = Color(0xFF6B6B7B);
  static const Color textLight = Color(0xFFFFFFFF);
  static const Color textHint = Color(0xFF9E9EAE);

  // ── Input fields ──────────────────────────────────────
  static const Color inputFill = Color(0xFFF2F0EC);
  static const Color inputFillDark = Color(0xFF2E2E48);
  static const Color inputBorder = Color(0xFFDAD6CE);

  // ── Feedback ──────────────────────────────────────────
  static const Color success = Color(0xFF2D6A4F);
  static const Color error = Color(0xFFC0392B);
  static const Color warning = Color(0xFFD4A017);

  // ── Misc ──────────────────────────────────────────────
  static const Color divider = Color(0xFFE0DDD6);
  static const Color shadow = Color(0x1A000000);
  static const Color iconCircleBg = Color(0x33FFFFFF);
}
